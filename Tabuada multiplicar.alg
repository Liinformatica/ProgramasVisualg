algoritmo "Tabuada"
// Função :
// Autor :
// Data : 06/04/2018
// Seção de Declarações 
var
mul, n: inteiro

inicio
// Seção de Comandos
mul <- 1

escreva ("Digite a tabuada:")
leia (n)

enquanto (mul <= 10) faca
   escreval (n, "*", mul, "=", n * mul)
   mul <- mul + 1
fimenquanto

fimalgoritmo
